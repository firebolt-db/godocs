[project]
name = "conv2mint"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "markdown-it-py==3.0.0",
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
