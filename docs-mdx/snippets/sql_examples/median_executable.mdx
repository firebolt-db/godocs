

import {QueryWindow} from '/snippets/query-window.mdx';

**Example**

The following query uses `UNNEST` to convert an array [1,2,5] into a column, calculates the median value, and returns it as `result`:




<QueryWindow content={{
  "sql": "SELECT MEDIAN(a) as result FROM UNNEST(array[1,2,5]) as a;",
  "result": {
    "data": [
      [
        2
      ]
    ],
    "meta": [
      {
        "name": "result",
        "type": "double null"
      }
    ],
    "query": {
      "query_id": "4acd5bdf-44ad-4806-87be-48367d86e489",
      "query_label": null,
      "request_id": "bef6b7fa-9a6e-4fc7-be31-92cc587e60c2"
    },
    "rows": 1,
    "statistics": {
      "bytes_read": 1,
      "elapsed": 0.007564,
      "rows_read": 1,
      "scanned_bytes_cache": 0,
      "scanned_bytes_storage": 0,
      "time_before_execution": 0.000253097,
      "time_to_execute": 9.8248e-05
    }
  }
}} />


**Returns**

The previous code example returns the middle element as the median because the number of elements in the array is odd.

**Example**

The following query uses `UNNEST` to convert an array [100, NULL, 1, 2, 5] into a column, calculates the median value, and returns it as `result`:



<QueryWindow content={{
  "sql": "SELECT MEDIAN(a) as result FROM UNNEST(array[100,NULL,1,2,5]) as a;",
  "result": {
    "data": [
      [
        3.5
      ]
    ],
    "meta": [
      {
        "name": "result",
        "type": "double null"
      }
    ],
    "query": {
      "query_id": "cd3e49c5-a31f-4f9c-906b-58dbaf03a0f5",
      "query_label": null,
      "request_id": "bb487fed-c5d8-45bd-b4ea-327e619b899b"
    },
    "rows": 1,
    "statistics": {
      "bytes_read": 1,
      "elapsed": 0.007283,
      "rows_read": 1,
      "scanned_bytes_cache": 0,
      "scanned_bytes_storage": 0,
      "time_before_execution": 0.000248316,
      "time_to_execute": 8.9024e-05
    }
  }
}} />


**Returns**

The previous code example returns the average of the two middle elements after sorting the values, because the number of non-`NULL` elements is even after ignoring `NULL` values. The average of the middle elements, `2` and `5`,  is calculated as : `((2+5)/2)`.




