

import {QueryWindow} from '/snippets/query-window.mdx';

**Example**

The following query uses `UNNEST` to convert an array [1,2,5] into a column, calculates the median value, and returns it as `result`:




<QueryWindow content={{"sql": "SELECT MEDIAN(a) as result FROM UNNEST(array[1,2,5]) as a;", "result": {"query": {"query_id": "5ee18123-95b2-4b29-a5da-51d7501405c4", "request_id": "b0d01496-ccd5-4aae-a599-36fed23539c4", "query_label": null}, "meta": [{"name": "result", "type": "double null"}], "data": [[2]], "rows": 1, "statistics": {"elapsed": 0.008398, "rows_read": 1, "bytes_read": 1, "time_before_execution": 0.000332557, "time_to_execute": 0.000105014, "scanned_bytes_cache": 0, "scanned_bytes_storage": 0}}}} />


**Returns**

The previous code example returns the middle element as the median because the number of elements in the array is odd.

**Example**

The following query uses `UNNEST` to convert an array [100, NULL, 1, 2, 5] into a column, calculates the median value, and returns it as `result`:



<QueryWindow content={{"sql": "SELECT MEDIAN(a) as result FROM UNNEST(array[100,NULL,1,2,5]) as a;", "result": {"query": {"query_id": "a0e20519-5d3c-4307-8a02-a4e00f6c8d2c", "request_id": "98094390-8825-45a6-875b-061ce683d065", "query_label": null}, "meta": [{"name": "result", "type": "double null"}], "data": [[3.5]], "rows": 1, "statistics": {"elapsed": 0.007823, "rows_read": 1, "bytes_read": 1, "time_before_execution": 0.000309723, "time_to_execute": 9.4504e-05, "scanned_bytes_cache": 0, "scanned_bytes_storage": 0}}}} />


**Returns**

The previous code example returns the average of the two middle elements after sorting the values, because the number of non-`NULL` elements is even after ignoring `NULL` values. The average of the middle elements, `2` and `5`,  is calculated as : `((2+5)/2)`.




