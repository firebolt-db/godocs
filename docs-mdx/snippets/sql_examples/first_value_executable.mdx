

import {QueryWindow} from '/snippets/query-window.mdx';

**Example**

The following query uses `UNNEST` to convert an array `[1,2,5]` into a column, order the values, and returns the first value as `result`:




<QueryWindow content={{
  "sql": "SELECT FIRST_VALUE(a) OVER (ORDER BY a) as result FROM UNNEST(array[1,2,5]) as a;",
  "result": {
    "data": [
      [
        1
      ],
      [
        1
      ],
      [
        1
      ]
    ],
    "meta": [
      {
        "name": "result",
        "type": "int null"
      }
    ],
    "query": {
      "query_id": "6c9519c4-9bf8-4e97-9f3c-a83af90bba49",
      "query_label": null,
      "request_id": "fe911df2-b7db-4bdf-8aef-22045c889a52"
    },
    "rows": 3,
    "statistics": {
      "bytes_read": 1,
      "elapsed": 0.006856,
      "rows_read": 1,
      "scanned_bytes_cache": 0,
      "scanned_bytes_storage": 0,
      "time_before_execution": 0.000538504,
      "time_to_execute": 0.00016431
    }
  }
}} />


**Returns**

The previous code example returns the first element after ordering.

**Example**

The following query uses `UNNEST` to convert an array `[100, NULL, 1]` into a column, order the values, and returns the first value as `result`:




<QueryWindow content={{
  "sql": "SELECT FIRST_VALUE(a) OVER (ORDER BY a desc nulls first) as result FROM UNNEST(array[100,NULL,1]) as a;",
  "result": {
    "data": [
      [
        null
      ],
      [
        null
      ],
      [
        null
      ]
    ],
    "meta": [
      {
        "name": "result",
        "type": "int null"
      }
    ],
    "query": {
      "query_id": "5fd2cdad-8878-43d5-ac8e-6f392e14138b",
      "query_label": null,
      "request_id": "69da2012-5bb0-4e96-88e7-9055b304d1fd"
    },
    "rows": 3,
    "statistics": {
      "bytes_read": 1,
      "elapsed": 0.006676,
      "rows_read": 1,
      "scanned_bytes_cache": 0,
      "scanned_bytes_storage": 0,
      "time_before_execution": 0.000536983,
      "time_to_execute": 0.00016231
    }
  }
}} />


**Returns**

The previous code example returns `NULL` values because `ORDER BY` specifies 'nulls first'.


