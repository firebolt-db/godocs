---
description: Reference material for LENGTH function
sidebarTitle: LENGTH
title: LENGTH
---


Calculates the length of the input string.

When used with an array argument, `LENGTH` is a synonym for [ARRAY_LENGTH](/reference-sql/functions-reference/array/array-length)

## Syntax


```sql
LENGTH(<expression>)
```
## Parameters


| Parameter      | Description                                  |Supported input types |
| :--------------| :--------------------------------------------|:----------------------|
| `<expression>` | The string or binary data for which to return the length.   | `TEXT`, `BYTEA`. For `ARRAY`, see [ARRAY_LENGTH](/reference-sql/functions-reference/array/array-length) |

## Return Type
`INTEGER`

## Example


Use the `LENGTH` to find the length of any string, such as:

```sql
SELECT LENGTH('The Accelerator Cup')
```
Spaces are included in the calculation of the total length of the string.

**Returns**: `19`

