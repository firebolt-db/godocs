The following query calculates the cosine of pi, which is -1:

{% include query-window.html sql_file="sql_examples/cos_example_0.sql" %}

The following query calculates the cosine of 0, which is 1:

{% include query-window.html sql_file="sql_examples/cos_example_1.sql" %}

