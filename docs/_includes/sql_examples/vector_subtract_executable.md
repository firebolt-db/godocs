**Example**

The following example subtracts the vector `[3, 4, -4]` from `[1, 5, 6]` by computing `1-3`, `5-4`, and `6-(-2)`, which yields `[-2, 1, 8]`:


{% include query-window.html sql_file="sql_examples/vector_subtract_example_0.sql" %}

**Returns**

